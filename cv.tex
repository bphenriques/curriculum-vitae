%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode

% Configure document
\documentclass[12pt, a4paper]{awesome-cv}
\geometry{left=1.4cm, top=.8cm, right=1.4cm, bottom=1.8cm, footskip=1.0cm}

% Absorb page slack into legibility (line-height in the text blocks) rather than
% inter-section gaps. Body baselineskip is set to 1em (ratio ~1.0), so this is a
% gentle spread. Tune upward (~1.10-1.12) to fill more, but keep it to ONE page.
%\linespread{1.02}

% ATS / copy-paste fix: make XeTeX record the INPUT characters as /ActualText, so
% the PDF text layer extracts what was typed ("Engineer", "Distributed Systems")
% regardless of glyph substitution. Without this, small-caps glyphs extract as a
% dotless-i / long-s and fail ATS keyword matching. Keeps the small-caps look.
\XeTeXgenerateactualtext=1

\input{content/info}

\begin{document}
    \makecvheader[L]
    % Easter egg: QR in the top-right corner, pointing to a static site self-hosted
    % in a homelab microVM. Absolute overlay so it doesn't disturb the header layout.
    % (Needs two compile passes for the position; latexmk handles that.)
    \begin{tikzpicture}[remember picture, overlay]
      \node[anchor=north east, inner sep=0pt]
        at ([xshift=-1.4cm, yshift=-0.8cm]current page.north east)
        {\includegraphics[width=1.5cm]{content/qrcode.png}};
    \end{tikzpicture}%
    \makecvfooter{Updated \today}{}{\thepage}
    \input{content/summary}
    \input{content/experience}
    \input{content/education}
    \input{content/skills}
    %\input{content/honors}
    %\newpage
    \input{content/open-source}
\end{document}

% Other interesting resumes:
% - https://kirintwn.github.io/resume/resume.pdf
